PORT=5000
CLIENT_URL=http://localhost:5173

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=les_database

JWT_SECRET=change_this_to_a_long_random_string_at_least_32_chars
JWT_EXPIRES_IN=7d

ADMIN_NAME=Hasna Faizar
ADMIN_EMAIL=admin@ruhuna.lk
ADMIN_PASSWORD=Admin@123
ADMIN_DEPARTMENT_ID=1
ADMIN_UNIVERSITY_ID=ADM001

# Optional: SMTP settings for email features (password resets, notifications)
# If omitted, the backend logs email content to console instead
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_gmail_app_password_without_spaces
SMTP_FROM="Lecturer Evaluation System <your_email@gmail.com>"

# Optional: MySQL binary path on Windows if mysqldump/mysql is not in your PATH
# Required for npm run db:export and npm run db:import
# MYSQL_BIN_DIR=C:\Program Files\MySQL\MySQL Server 8.0\bin
